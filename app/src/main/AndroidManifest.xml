<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xpq.a1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".doingActivity"
            android:label="@string/doing_name" />
        <activity
            android:name=".proActivity"
            android:label="@string/pro_name" />
        <activity
            android:name=".settingActivity"
            android:theme="@style/AppTheme.NoBar" />
        <activity android:name=".countActivity"
            android:label="@string/count_name" ></activity>
    </application>

</manifest>